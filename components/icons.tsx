import { Briefcase, Moon, SunMedium, User, Settings, LogOut, LayoutDashboard } from "lucide-react"
import type { LucideIcon } from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  logo: Briefcase,
  sun: SunMedium,
  moon: Moon,
  user: User,
  settings: Settings,
  logout: LogOut,
  dashboard: LayoutDashboard,
}

